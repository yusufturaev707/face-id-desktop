PyQt6>=6.6.0
opencv-python>=4.9.0
insightface>=0.7.3
numpy>=1.26.0
httpx>=0.27.0
PyJWT>=2.8.0
Pillow>=10.2.0
pygrabber>=0.2

# ONNX Runtime — faqat BIRINI install qiling:
# GPU bor kompyuter uchun (CUDA 12.x):
#   pip install onnxruntime-gpu==1.20.1
# GPU yo'q kompyuter uchun (CPU only):
#   pip install onnxruntime==1.20.1
onnxruntime==1.20.1

# PyTorch — GPU (CUDA 12.1) orqali hisoblash uchun:
#   pip install torch==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# GPU yo'q bo'lsa CPU versiya avtomatik ishlatiladi:
#   pip install torch==2.5.1
torch>=2.5.1
